/// <reference path="bs/definitions/definitions.d.ts" />

$(document).ready(() => {

    let bsSetup = new bs.components.BattleshipSetup();
    // let bsGame = new bs.components.BattleshipGame();

    $("[data-toggle=\"tooltip\"]").tooltip();

});
